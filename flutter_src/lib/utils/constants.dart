import 'package:flutter/material.dart';

const kEmailTextFieldDecoration = InputDecoration(
  hintText: kEmailTextFieldHintText,
);

const kPasswordTextFieldDecoration =
    InputDecoration(hintText: kPasswordTextFieldHintText);

const kEmailTextFieldStyle = TextStyle(
  fontSize: 20,
);

const kEmailTextFieldHintText = "name@example.com";
const kPasswordTextFieldHintText = "yourpassword";
